{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "env": {
    "SMTP_USER": "@smtp_user",
    "SMTP_PASSWORD": "@smtp_password",
    "ADMIN_EMAIL": "@admin_email"
  },
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs20.x"
    }
  }
}
